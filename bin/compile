#!/bin/sh
echo "🔥 Running final slug audit & cleanup"

echo "📦 Top folders before compression:"
du -h -d 2 /app | sort -hr | head -n 20

# Deleta .bundle agora, já que vendor/bundle não é mais usado
echo "🧹 Removing /app/.bundle..."
rm -rf /app/.bundle

echo "✅ Done. Top folders after cleanup:"
du -h -d 2 /app | sort -hr | head -n 20